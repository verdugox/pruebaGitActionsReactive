pipeline {
    agent any

    environment {
        MAVEN_HOME = tool 'Maven 3.9.9'
        JAVA_HOME = tool 'JDK17'
        RAILWAY_TOKEN = credentials('railway-token') // Token almacenado en Jenkins
        DEPLOY_ENV = "prod"
        CONFIG_FILE = "devops/deploy/prod-vars.yaml"
        RAILWAY_SERVICE_ID = "d1d288ff-c26f-4879-8ae6-a32c921b9a00" // Reemplaza con el nombre o ID del servicio
    }

    stages {
        stage('Preparation') {
            steps {
                echo "Preparing environment..."
                checkout scm
                bat "${MAVEN_HOME}\\bin\\mvn --version"
            }
        }

        stage('Build') {
            steps {
                dir('client-service') {
                    bat "${MAVEN_HOME}\\bin\\mvn clean package -DskipTests"
                }
            }
        }

        stage('Verify JAR File') {
            steps {
                dir('client-service') {
                    bat "dir target"
                }
            }
        }

        stage('Verify Railway CLI') {
            steps {
                echo "Verifying Railway CLI installation..."
                bat '''
                railway --version
                '''
            }
        }

        stage('Deploy to Railway') {
            steps {
                echo "Deploying application to Railway..."
                dir('client-service/target') {
                    bat """
                    set RAILWAY_TOKEN=%RAILWAY_TOKEN%
                    railway up --detach --service=%RAILWAY_SERVICE_ID%
                    """
                }
            }
        }
    }

    post {
        success {
            echo "Pipeline completed successfully!"
        }
        failure {
            echo "Pipeline failed. Check the logs."
        }
    }
}
